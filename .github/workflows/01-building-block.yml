name: 01-building-blocks

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Echo Hello World
              run: echo "Hello, World!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Falied Step
              run: |
                echo "This step will succeed."
                exit 0
            - name: Echo Goodbye World
              run: echo "Goodbye, World!"          